# Architecture boundary tests

These tests enforce that packaged applications/services communicate through APIs instead of sharing Python runtimes, virtual environments, modules, or internal script paths.
